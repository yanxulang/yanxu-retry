引「../src/言韧.yx」为 言韧；

引「标准:测试」为 测试；

定 策略 为 言韧.重试策略（4，0.25，2，1）；

定 各延迟：列<数> 为 言韧.延迟表（策略）；

测试.相等（长度（各延迟），3）；

测试.相等（各延迟【0】，0.25）；

测试.相等（各延迟【1】，0.5）；

测试.相等（各延迟【2】，1）；

令 调用数：数 为 0；

法 第三次成功（次数：数）：文 则
    置 调用数 为 （调用数 加 1）；
    若 （次数 小于 3） 则
        抛 「暂时失败」；
    终
    归 「完成」；
终

测试.相等（言韧.立即执行（策略，第三次成功，言韧.总是重试），「完成」）；

测试.相等（调用数，3）；

令 失败调用：数 为 0；

法 总失败（次数：数）：文 则
    置 失败调用 为 （失败调用 加 1）；
    抛 「始终失败」；
终

定 结果：典 为 言韧.尝试执行（言韧.立即策略（2），总失败，言韧.总是重试，假）；

测试.断言（非 结果【「成功」】，「最终错误应转为结果」）；

测试.相等（结果【「次数」】，2）；

测试.相等（失败调用，2）；

测试.断言（（结果【「错误」】【「消息」】 等于 「始终失败」），「应保留错误消息」）；
