引「../src/言韧.yx」为 言韧；

引「标准:测试」为 测试；

令 各等待：列<数> 为 【】；

令 各阶段：列<文> 为 【】；

法 记录等待（秒：数，上下文：典）：空 则
    追加（各等待，秒）；
    测试.相等（上下文【「阶段」】，「等待」）；
    归 空；
终

法 记录事件（事件：典）：空 则
    测试.相等（事件【「模式版本」】，言韧.事件模式版本）；
    追加（各阶段，事件【「阶段」】）；
    归 空；
终

法 不停止（上下文：典）：理 则
    归 假；
终

令 调用数：数 为 0；

法 第三次完成（次数：数）：文 则
    置 调用数 为 （调用数 加 1）；
    若 （次数 小于 3） 则
        抛 「暂时故障」；
    终
    归 「完成」；
终

定 控制 为 言韧.执行控制（）.设等待器（记录等待）.设观察器（记录事件）.设停止判定（不停止）；

测试.相等（言韧.受控执行（言韧.重试策略（4，0.5，2，2），第三次完成，言韧.总是重试，控制），「完成」）；

测试.相等（调用数，3）；

测试.相等（长度（各等待），2）；

测试.相等（各等待【0】，0.5）；

测试.相等（各等待【1】，1）；

测试.相等（长度（各阶段），8）；

测试.相等（各阶段【0】，「开始」）；

测试.相等（各阶段【1】，「失败」）；

测试.相等（各阶段【2】，「等待」）；

测试.相等（各阶段【7】，「成功」）；

令 停止调用：数 为 0；

法 一直失败（次数：数）：空 则
    置 停止调用 为 （停止调用 加 1）；
    抛 「仍未完成」；
终

法 两次后停止（上下文：典）：理 则
    归 （上下文【「次数」】 大于 2）；
终

定 停止结果：典 为 言韧.受控尝试执行（言韧.立即策略（5），一直失败，言韧.总是重试，言韧.立即执行控制（）.设停止判定（两次后停止））；

测试.断言（非 停止结果【「成功」】，「停止应返回结构化失败」）；

测试.相等（停止结果【「错误」】【「代码」】，「YANREN_STOPPED」）；

测试.相等（停止结果【「次数」】，2）；

测试.相等（停止调用，2）；

法 判定失败（所误：误，次数：数）：理 则
    抛 「判定器损坏」；
终

定 判定结果：典 为 言韧.受控尝试执行（言韧.立即策略（2），一直失败，判定失败，言韧.立即执行控制（））；

测试.相等（判定结果【「错误」】【「代码」】，「YANREN_PREDICATE」）；

法 等待失败（秒：数，上下文：典）：空 则
    抛 「调度器损坏」；
终

定 等待结果：典 为 言韧.受控尝试执行（言韧.重试策略（2，0.1，1，0.1），一直失败，言韧.总是重试，言韧.执行控制（）.设等待器（等待失败））；

测试.相等（等待结果【「错误」】【「代码」】，「YANREN_WAITER」）；

令 成功操作数：数 为 0；

法 一次成功（次数：数）：文 则
    置 成功操作数 为 （成功操作数 加 1）；
    归 「值」；
终

法 成功事件失败（事件：典）：空 则
    若 （事件【「阶段」】 等于 「成功」） 则
        抛 「指标系统损坏」；
    终
    归 空；
终

定 观察结果：典 为 言韧.受控尝试执行（言韧.立即策略（3），一次成功，言韧.总是重试，言韧.立即执行控制（）.设观察器（成功事件失败））；

测试.相等（观察结果【「错误」】【「代码」】，「YANREN_OBSERVER」）；

测试.相等（成功操作数，1）；
