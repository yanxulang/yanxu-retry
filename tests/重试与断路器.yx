引「../src/言韧.yx」为 言韧；

引「标准:测试」为 测试；

令 当前毫秒：数 为 0；

令 各等待：列<数> 为 【】；

法 虚拟时间（）：数 则
    归 当前毫秒；
终

法 推进等待（秒：数，上下文：典）：空 则
    追加（各等待，秒）；
    置 当前毫秒 为 （当前毫秒 加 （秒 乘 1000））；
    归 空；
终

令 服务调用：数 为 0；

法 暂时故障（次数：数）：文 则
    置 服务调用 为 （服务调用 加 1）；
    若 （次数 小于 3） 则
        抛 「YANGATE_UNAVAILABLE：上游暂不可用」；
    终
    归 「已恢复」；
终

定 所断路器 为 言韧.断路器（2，1000）.设时间源（虚拟时间）；

定 控制 为 言韧.执行控制（）.设等待器（推进等待）；

测试.相等（言韧.受控执行经断路器（言韧.重试策略（4，0.5，2，1），所断路器，暂时故障，言韧.总是重试，控制），「已恢复」）；

测试.相等（服务调用，3）；

测试.相等（当前毫秒，1500）；

测试.相等（各等待【0】，0.5）；

测试.相等（各等待【1】，1）；

定 断路快照：典 为 所断路器.快照（）；

测试.相等（断路快照【「状态」】，言韧.关闭状态）；

测试.相等（断路快照【「失败总数」】，2）；

测试.相等（断路快照【「成功总数」】，1）；

测试.相等（断路快照【「开启总数」】，1）；

令 代码调用：数 为 0；

法 超时后成功（次数：数）：文 则
    置 代码调用 为 （代码调用 加 1）；
    若 （次数 等于 1） 则
        抛 「YANQIU_TIMEOUT：请求超时」；
    终
    归 「响应」；
终

令 代码配置：列<文> 为 【「YANQIU_TIMEOUT」】；

定 超时判定：法 为 言韧.可重试代码（代码配置）；

置 代码配置【0】 为 「YANQIU_CHANGED」；

测试.相等（言韧.立即执行（言韧.立即策略（2），超时后成功，超时判定），「响应」）；

测试.相等（代码调用，2）；

法 永久失败（次数：数）：空 则
    抛 「YANQIU_INVALID：请求无效」；
终

定 永久结果：典 为 言韧.尝试执行（言韧.立即策略（3），永久失败，超时判定，假）；

测试.断言（非 永久结果【「成功」】，「未列入的错误不应重试」）；

测试.相等（永久结果【「次数」】，1）；

测试.相等（永久结果【「错误」】【「代码」】，「YANQIU_INVALID」）；

法 非法代码（）：空 则
    言韧.可重试代码（【「timeout」】）；
    归 空；
终

令 代码失败：理 为 假；

试 则
    非法代码（）；
救 所误 则
    置 代码失败 为 （言韧.错误详情（所误）【「代码」】 等于 「YANREN_PREDICATE_CODE」）；
终

测试.断言（代码失败，「非法错误代码应被拒绝」）；
