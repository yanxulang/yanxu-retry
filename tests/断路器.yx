引「../src/言韧.yx」为 言韧；

引「标准:测试」为 测试；

定 所断路器 为 言韧.断路器（2，60000）；

测试.相等（所断路器.状态（），「关闭」）；

所断路器.记失败（）；

测试.相等（所断路器.状态（），「关闭」）；

所断路器.记失败（）；

测试.相等（所断路器.状态（），「开启」）；

测试.断言（非 所断路器.是否允许（），「开启时应拒绝调用」）；

所断路器.记成功（）；

测试.相等（所断路器.状态（），「关闭」）；

测试.相等（所断路器.失败数（），0）；
