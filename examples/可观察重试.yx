引「../src/言韧.yx」为 言韧；

令 各等待：列<数> 为 【】；

令 各阶段：列<文> 为 【】；

法 外部调度（秒：数，上下文：典）：空 则
    追加（各等待，秒）；
    归 空；
终

法 记录阶段（事件：典）：空 则
    追加（各阶段，事件【「阶段」】）；
    归 空；
终

法 第二次成功（次数：数）：文 则
    若 （次数 等于 1） 则
        抛 「YANQIU_TIMEOUT：请求超时」；
    终
    归 「响应成功」；
终

定 控制 为 言韧.执行控制（）.设等待器（外部调度）.设观察器（记录阶段）；

定 结果 为 言韧.受控执行（言韧.重试策略（3，0.25，2，1）.设抖动（0.5，2026），第二次成功，言韧.可重试代码（【「YANQIU_TIMEOUT」】），控制）；

言 结果；

言 各等待；

言 各阶段；
