引「../src/言韧.yx」为 言韧；

令 当前毫秒：数 为 0；

法 虚拟时间（）：数 则
    归 当前毫秒；
终

法 推进等待（秒：数，上下文：典）：空 则
    置 当前毫秒 为 （当前毫秒 加 （秒 乘 1000））；
    归 空；
终

法 第三次恢复（次数：数）：文 则
    若 （次数 小于 3） 则
        抛 「YANGATE_UNAVAILABLE：上游暂不可用」；
    终
    归 「上游恢复」；
终

定 所断路器 为 言韧.断路器（2，1000）.设时间源（虚拟时间）；

定 控制 为 言韧.执行控制（）.设等待器（推进等待）；

言 言韧.受控执行经断路器（言韧.重试策略（4，0.5，2，1），所断路器，第三次恢复，言韧.总是重试，控制）；

言 所断路器.状态（）；

言 当前毫秒；
