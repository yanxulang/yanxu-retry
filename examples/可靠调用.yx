引「../src/言韧.yx」为 言韧；

令 次数：数 为 0；

法 不稳定操作（尝试：数）：文 则
    置 次数 为 （次数 加 1）；
    若 （尝试 小于 3） 则
        抛 「YANGATE_UNAVAILABLE：服务暂不可用」；
    终
    归 「服务已恢复」；
终

定 可恢复：法 为 言韧.可重试代码（【「YANGATE_UNAVAILABLE」】）；

言 言韧.立即执行（言韧.默认策略（），不稳定操作，可恢复）；
